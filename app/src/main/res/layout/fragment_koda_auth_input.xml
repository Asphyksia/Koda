<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:background="@color/koda_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="24dp">

        <!-- Back button and title -->
        <TextView
            android:id="@+id/auth_input_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/koda_back_arrow"
            android:textSize="14sp"
            android:textColor="@color/koda_accent"
            android:clickable="true"
            android:focusable="true"
            android:padding="8dp"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/auth_input_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/koda_provider_auth"
            android:textSize="24sp"
            android:textStyle="bold"
            android:textColor="@color/koda_on_background"
            android:layout_marginBottom="24dp" />

        <!-- Instructions (varies by auth method) -->
        <TextView
            android:id="@+id/auth_input_instructions"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/koda_secondary_text"
            android:lineSpacingMultiplier="1.3"
            android:layout_marginBottom="24dp" />

        <!-- Input field for API Key / Setup Token -->
        <LinearLayout
            android:id="@+id/auth_input_field_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="visible">

            <TextView
                android:id="@+id/auth_input_label"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/koda_api_key"
                android:textSize="14sp"
                android:textStyle="bold"
                android:textColor="@color/koda_on_background"
                android:layout_marginBottom="8dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:background="@drawable/koda_input_bg"
                android:padding="4dp">

                <EditText
                    android:id="@+id/auth_input_field"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:hint="@string/koda_paste_key_here"
                    android:inputType="textPassword"
                    android:background="@android:color/transparent"
                    android:textColor="@color/koda_on_background"
                    android:textColorHint="@color/koda_secondary_text"
                    android:padding="12dp" />

                <!-- Toggle visibility button -->
                <ImageButton
                    android:id="@+id/auth_input_toggle_visibility"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:src="@drawable/ic_visibility"
                    android:background="?android:attr/selectableItemBackgroundBorderless"
                    android:tint="@color/koda_secondary_text"
                    android:contentDescription="@string/koda_toggle_visibility" />

            </LinearLayout>

        </LinearLayout>

        <!-- OAuth button (shown for OAuth auth) -->
        <Button
            android:id="@+id/auth_input_oauth_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/koda_open_browser_signin"
            android:visibility="gone"
            android:layout_marginBottom="16dp"
            android:background="@drawable/koda_button_bg"
            android:textColor="#1A1A1A"
            android:textStyle="bold" />

        <!-- Status message (success/error) -->
        <LinearLayout
            android:id="@+id/auth_input_status_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@drawable/koda_card_bg"
            android:padding="12dp"
            android:layout_marginTop="16dp"
            android:visibility="gone">

            <TextView
                android:id="@+id/auth_input_status_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="14sp"
                android:textColor="@color/koda_on_background" />

        </LinearLayout>

        <!-- Verify and Continue button -->
        <Button
            android:id="@+id/auth_input_verify_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/koda_verify_continue"
            android:layout_marginTop="24dp"
            android:background="@drawable/koda_button_bg"
            android:textColor="#1A1A1A"
            android:textStyle="bold" />

    </LinearLayout>

</ScrollView>
