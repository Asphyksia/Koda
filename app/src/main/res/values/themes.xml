<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
     https://material.io/develop/android/theming/dark
    -->

    <!-- TermuxApp Light DarkActionBar theme. -->
    <style name="Theme.TermuxApp.Light.DarkActionBar" parent="Theme.BaseActivity.Light.DarkActionBar"/>
    <!-- TermuxApp Light NoActionBar theme. -->
    <style name="Theme.TermuxApp.Light.NoActionBar" parent="Theme.BaseActivity.Light.NoActionBar"/>

    <!-- TermuxApp DayNight DarkActionBar theme. -->
    <style name="Theme.TermuxApp.DayNight.DarkActionBar" parent="Theme.BaseActivity.DayNight.DarkActionBar"/>
    <!-- TermuxApp DayNight NoActionBar theme. -->
    <style name="Theme.TermuxApp.DayNight.NoActionBar" parent="Theme.BaseActivity.DayNight.NoActionBar"/>


    <!-- TermuxActivity DayNight NoActionBar theme. -->
    <style name="Theme.TermuxActivity.DayNight.NoActionBar" parent="Theme.TermuxApp.DayNight.NoActionBar">
        <!-- Koda primary brand color. -->
        <item name="colorPrimary">@color/koda_background</item>
        <item name="colorPrimaryDark">#0B1120</item>
        <item name="colorPrimaryVariant">@color/koda_background</item>
        <item name="colorAccent">@color/koda_accent</item>

        <item name="android:windowBackground">@color/koda_background</item>
        <item name="android:textColor">@color/koda_on_background</item>
        <item name="android:textColorPrimary">@color/koda_on_background</item>
        <item name="android:textColorSecondary">@color/koda_secondary_text</item>
        <item name="android:statusBarColor">#0B1120</item>
        <item name="android:navigationBarColor">#0B1120</item>

        <!-- Avoid action mode toolbar pushing down terminal content when
             selecting text on pre-6.0 (non-floating toolbar). -->
        <item name="android:windowActionModeOverlay">true</item>

        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>

        <!-- https://developer.android.com/training/tv/start/start.html#transition-color -->
        <item name="android:windowAllowReturnTransitionOverlap">true</item>
        <item name="android:windowAllowEnterTransitionOverlap">true</item>

        <!-- Left drawer. -->
        <item name="buttonBarButtonStyle">@style/TermuxActivity.Drawer.ButtonBarStyle.Dark</item>
        <item name="termuxActivityDrawerBackground">@color/koda_surface</item>
        <item name="termuxActivityDrawerImageTint">@color/koda_accent</item>

        <!-- Extra keys colors. -->
        <item name="extraKeysButtonTextColor">@color/koda_on_background</item>
        <item name="extraKeysButtonActiveTextColor">@color/koda_accent</item>
        <item name="extraKeysButtonBackgroundColor">@color/koda_surface</item>
        <item name="extraKeysButtonActiveBackgroundColor">@color/koda_accent_dark</item>
    </style>

</resources>
